
namespace HyperDownloadManager.Dialogs.MessageBoxDialog
{
    public enum MessageBoxStatus
    {
        NONE,
        OK,
        CANCEL,
        IGNORE,
        YES,
        NO,
        CONTINUE,
        ABORT
    }
}
